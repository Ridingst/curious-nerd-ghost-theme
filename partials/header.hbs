<header class="site-header">
	<div role='banner' class="header_teq">
		{{#if @blog.logo}}<a class="logo_teq" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
		<h1 class="blogtitle_teq">
			<a title="{{@blog.title}}" href='{{@blog.url}}'>
				{{{@blog.title}}}
			</a>
		</h1>
		<h2 class="blogdescription_teq">
			<a title="{{@blog.title}}" href='{{@blog.url}}'>
				{{@blog.description}}
			</a>
		</h2>
	</div>
</header>

